# Dependencias (esta es la más importante, es una carpeta pesada)
node_modules/
#test
# Archivos de producción (la carpeta que se crea al hacer npm run build)
dist/
dist-ssr/
*.local

# Variables de entorno (si llegas a usar .env en el front)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs de errores
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos del sistema (especialmente para ti que usas macOS)
.DS_Store

# Configuración de editores
.vscode/
.idea/